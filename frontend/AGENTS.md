# Frontend provenance

This frontend originated in a Lovable-connected repository. The combined
`enclave-full` repository is independent and has no configured Lovable sync.
Do not assume a push here publishes the original website. Preserve published
Git history and keep changes to the original upstream repository separate.
